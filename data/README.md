# /data

此目录存放航班数据 JSON 文件。

当 `index.html` 中的 `API_CONFIG.USE_MOCK` 设为 `false` 时，前端会从此目录读取数据。

## 文件说明

| 文件 | 用途 |
|------|------|
| `deals.json` | 首页特价航班列表 |
| `search/*.json` | 各航线搜索结果（按航线分文件） |

数据由爬虫脚本或手动更新生成，推送到此目录后前端自动读取。
